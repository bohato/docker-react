sudo: required
services:
    - docker

before_install:
    - docker build -t bohato/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true bohato/docker-react npm run test

language: generic 